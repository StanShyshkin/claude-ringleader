# Task artifacts (generated per-task, not version controlled)
artifacts/*/

# Event logs
logs/*.jsonl

# Last task ID (written by delegate.sh for background task discovery)
.last-task-id

# Keep directory structure
!artifacts/.gitkeep
!logs/.gitkeep
